<h2>Books</h2>

<!-- サイドバーのUserinfoの下 -->
<h2>New Book</h2>
<%= form_with model: @book do |f| %>
  <label>Title</label>
  <%= f.text_field :title %>
  <label>Opnion</label>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>

