<%= form_with model: chat, url: chats_path, local: false do |f| %>
  <%= f.text_field :message %>
  <%= f.hidden_field :room_id %>
  <%= f.submit "SEND", class:"btn btn-sm btn-success chat-btn" %>
<% end %>