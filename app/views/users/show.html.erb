<h2>Books</h2>

<div>
  <label>Title</label>
  <lavel>Body</lavel>

  <% @books.each do |book| %>
    <%= link_to book_path(book.id) do %>
      <p><%= book.title %></p>
    <% end %>
    <p><%= book.body %></p>
  <% end %>
</div>


<%= render 'user_new', user: @user, book: @book %>
